
// ----> Estado Inicial <----
const initialState = {
    team: [],
    teamRespaldo: []
}

// ---> REDUCER <-----

const reducer = (state = initialState, { type, payload }) => {
    switch (type) {
  
        default:
            return {...state}
    }
}

export default reducer